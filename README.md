# python-RECOMMENDATION_SYSTEM-machine_learning-AI
A recommendation system that uses machine learning to recommend a movie the user would like most

# dependencies
pip install numpy
pip install scipy
pip install lightfm (requires visual c++ 14+ with sdk)

# IDE
pyCharm
